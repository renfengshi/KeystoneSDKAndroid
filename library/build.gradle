plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'maven-publish'
//    id 'org.mozilla.rust-android-gradle.rust-android'
}

group = 'com.keystone'
version = '1.5'

android {
    compileSdkVersion 31
    ndkVersion "25.2.9519653"

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 31
        versionCode 15
        versionName "1.5"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation 'androidx.core:core-ktx:1.7.0'
    implementation 'androidx.appcompat:appcompat:1.4.1'
    implementation 'com.google.android.material:material:1.5.0'
    api 'com.sparrowwallet:hummingbird:1.6.6'
    api 'com.google.code.gson:gson:2.8.6'
}

preBuild {
    doFirst {
        exec {
            executable "./env.sh" // set env for cargo python rustc
        }
    }
}

//cargo {
//    module = "./keystone-sdk-rust"       // Or whatever directory contains your Cargo.toml
//    libname = "ur-registry-ffi"          // Or whatever matches Cargo.toml's [package] name.
//    targets = ["arm", "x86", "arm64", "x86_64"]
//    profile = 'release'
//    targetIncludes = ['libur_registry_ffi.so']
////    rustupChannel = "nightly"
//}

afterEvaluate {
    publishing {
        publications {
            // Creates a Maven publication called "release".
            release(MavenPublication) {
                from components.release
                groupId = 'com.keystone'
                artifactId = 'sdk'
                version = '1.5'
            }
        }
    }
}

//tasks.whenTaskAdded { task ->
//    if ((task.name == 'javaPreCompileDebug' || task.name == 'javaPreCompileRelease')) {
//        task.dependsOn 'cargoBuild'
//    }
//}
